
csv_files <- list.files("ScenarioFiles", pattern = "\\.csv$", full.names = TRUE)

for (csv_file in csv_files) {
  
  file_path <- csv_file
  file_name <- basename(csv_file)
  
  # --- Your code below ---
  
  # --- End of your code ---
  
}
